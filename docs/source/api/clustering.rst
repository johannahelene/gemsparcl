gemsparcl.clustering
====================

Reads the distance file from sketchlib, builds a similarity network, and extracts connected-component clusters.
Distance files are processed in parallel chunks to handle datasets too large to fit in memory.

.. automodule:: gemsparcl.clustering
   :members:
   :undoc-members: False
   :show-inheritance:
